# Generated evidence

Harness runs write immutable proof directories below this path. Raw runs are ignored;
only this convention note is tracked. A run is authoritative only when its directory
contains `run.json`, `records.jsonl`, `summary.json`, and the atomic `COMPLETE` marker.
